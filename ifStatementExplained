*
 * 
 * 
 * 
 * @author Joseph Kirkish
 * date 06/03/2015
 */

public class ifStatementExplained {

	public static void main(String[] args) {
		
		//a single selection if statement operates on the principle of whether or not a condition is true or false
		//example
		int age = 15;//variable is an int type initialized to be 15
		
		if (age >= 21 )//conditional statement in parentheses read if age is greater than or equal to 21
		{
			//If someone's age is true to be greater than or equal to 21, this print statement executes
			System.out.println("Yes, you are allowed to go drinking with your girlfriends");
		}
          //if the conditional statement is false, then nothing is executed and the if statement is ignored
		//this if statement turns out to be false because 15 is less than 21. So it is ignored. Nothing prints
	}

}
